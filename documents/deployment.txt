Create a pi system sd card
Boot up the new system and set up the initial values
enable ssh
 sudo raspi-config
Download updates (on the target machine if possible)
 sudo apt-get update
 sudo apt full-upgrade
 sudo apt-get update
sudo apt full-upgrade

Build a new app release
 python utilities/build_app_release.py version

Transfer the app release to the new system
 scp releases/photoframe-0.1.0.zip pi@192.168.1.nnn:/home/pi/photoframe

Log on to the new system
 ssh

Unzip the code
 unzip -o


Move release/start-photoframe.sh
To the photoframe folder
Edit it
Make it executable
 chmod +x start-photoframe.sh

Add the sh file to the autostart
 nano ~/.config/labwc/autostart
and add
 /home/pi/photoframe/start-photoframe.sh &
Make it executable
 chmod +x ~/.config/labwc/autostart

Move release/project_config.json to /home/pi/photoframe
and edit the values

reboot